import { MatchesStringPipe } from './matches-string.pipe';

describe('MatchesStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchesStringPipe();
    expect(pipe).toBeTruthy();
  });
});
